extends: rouse_ring_bond.yaml

parallel:
  workers: 1
  log_dir: logs/rouse_ring_bond_video

save:
  burn_in_tau: 5.0
  until_tau: 10.0
  interval_tau: 0.01

output:
  experiment_name: rouse_ring_bond_video
