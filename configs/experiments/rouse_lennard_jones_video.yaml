extends: rouse_lennard_jones.yaml

parallel:
  workers: 1
  log_dir: logs/rouse_lennard_jones_video

save:
  burn_in_tau: 0.0
  until_tau: 5.0
  interval_tau: 0.01

output:
  experiment_name: rouse_lennard_jones_video
