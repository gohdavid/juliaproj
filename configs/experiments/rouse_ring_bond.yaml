extends: rouse_base.yaml
seed: 26

parallel:
  log_dir: logs/rouse_ring_bond

rouse:
  initialization: ring
  init_noise_std: 0.02

nonideal:
  excluded_volume:
    enabled: true
  confinement:
    enabled: true
  ring_bond:
    enabled: true

output:
  experiment_name: rouse_ring_bond
