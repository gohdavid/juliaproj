experiment:
  config: configs/experiments/rouse_ring_bond.yaml

data:
  prefer_hdf5: true

analysis:
  video: false
  potential: true
  score: true

score:
  bins: 120
  analytic_points: 1200

output:
  dir: outputs
  experiment_name: rouse_ring_bond
  histogram_file: rouse_ring_bond_potential_histogram.png
  score_file: rouse_ring_bond_score_norm2_histogram.png
