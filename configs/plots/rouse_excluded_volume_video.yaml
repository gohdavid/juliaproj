experiment:
  config: configs/experiments/rouse_excluded_volume_video.yaml

data:
  prefer_hdf5: true

analysis:
  video: true
  potential: false
  score: false

video:
  framerate: 60

output:
  dir: outputs
  experiment_name: rouse_excluded_volume_video
  frame_dirname: frames
  gif_file: rouse_excluded_volume_video_first_5tau.gif
