extends: rouse_base.yaml
seed: 24

parallel:
  log_dir: logs/rouse_nonideal_additive

nonideal:
  lennard_jones:
    enabled: true
  excluded_volume:
    enabled: true
  confinement:
    enabled: true

output:
  experiment_name: rouse_nonideal_additive
