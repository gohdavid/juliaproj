extends: rouse_nonideal_additive.yaml

parallel:
  workers: 1
  log_dir: logs/rouse_nonideal_additive_video

save:
  burn_in_tau: 5.0
  until_tau: 10.0
  interval_tau: 0.01

output:
  experiment_name: rouse_nonideal_additive_video
