experiment:
  family: nbody_flow_matching
  name: nbody_polymer_flow_matching_egnn_rouse_hdf5
  seed: 0

runtime:
  use_gpu: true

data:
  kind: rouse_hdf5
  dim: 2
  n_atoms: 32
  n_samples: 4000
  burn_in: 0
  source_dir: /orcd/scratch/orcd/006/davidgoh/juliaproj/outputs/rouse_base/e6a343e8490d/trajectories
  source_pattern: traj_seed*.h5
  allow_partial: false
  physics_params: [1.0, 1.0, 3.0]
  center: true

model:
  name: flow_matching_egnn
  equivariant: true
  hidden_dims: [64, 64, 64]

training:
  epochs: 60
  batch_size: 64
  learning_rate: 1.0e-3
  log_every: 16
  checkpoint_every: 5

sampling:
  n_samples: 1000
  steps: 64

output:
  dir: runs
  plots: true
